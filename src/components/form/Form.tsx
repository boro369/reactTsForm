import React, { FC } from 'react';
import './Form.css';

export const Form: FC = () => {
    return (
        <div className="form" />
    );
};

export const FormItem: FC = () => {
    return (
        <div className='form-item' />
    );
};
